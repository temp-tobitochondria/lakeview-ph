{
  "name": "lakeview-embedding-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local embeddings HTTP server using @xenova/transformers (no Python)",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.0",
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
